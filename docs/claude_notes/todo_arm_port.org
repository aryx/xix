# -*- org -*-
# Progress tracker for docs/claude_notes/notes_arm_port_plan.txt.
# See that file for rationale/design; this file is just checklist state.

* Phase 0 -- harness
** DONE diff harness script (assemble+link both sides, cmp -l, qemu-arm)
   scripts/diff-arm.sh + test-arm.sh (bash, not Testo -- see plan doc).
** DONE fix o5l exit-code propagation on error node "TODO: ..."
   Verified, not actually broken: CLI.ml's error handler already
   catches `Failure s` (what `error node`/`failwith` raise) and
   returns `Exit.Code 1`; anything else (e.g. Match_failure) just
   bubbles out of Exit.catch and gets OCaml's default non-zero exit.
   Confirmed empirically: a DIV instruction (Codegen5.ml's
   `Arith ((DIV|MOD), _, _, _, _) -> error node "TODO: DIV/MOD"`)
   makes o5l print the error and exit 1, not 0.
** DONE ELF layout parity (section-header presence, program-header count)
   Confirmed NOT the qemu-crash cause (the two LOAD segments already
   byte-matched goken). Implemented in Elf.ml/Elf.mli/Execgen.ml: the
   3rd (NOPTYPE/symtab-placeholder) program header, and the .text/
   .data/.strtab section header table + string table, both ported
   from goken's linkers/8l/elf.c (elf32/elf32sectab) -- every constant
   (offsets, the 0x10000 section addralign, the exact 22-byte string
   table content incl. goken's own double-trailing-NUL quirk in
   elfstrtab(), the "sh_size=14 doesn't cover it" mismatch) traced
   there, not guessed (first attempt got the string table 1 byte
   short -- caught by exit_linux.s, see Phase 0.5). Also added
   `-s` to the harness's goken invocation to strip goken's native
   Plan9 symbol/debug table (embeds the invocation cwd, not
   byte-reproducible, and xix never emits one anyway).

* Phase 0.5 -- baseline corpus (constructs already ported)
** DONE fix Codegen5.ml's immrot + offset_to_R12 (address-of-global)
   Both were already-flagged stubs ("less: x - BIG at some point",
   "TODO: port full code of immrot") -- finished properly rather than
   patched around, since a real immrot (full 8-bit-rotated-immediate
   search) is needed generally, not just for this one case. Ported
   goken's actual algorithm (5l/span.c immrot(): try each of the 16
   even rotations, unsigned 32-bit arithmetic) and the BIG bias
   ((1<<12)-4, 5l/l.h) that offset_to_R12 was missing. Caught by
   kitchen_sink.s (below) using two GLOBLs: with only one global at
   offset 0 (every earlier fixture), xix's old "offset=0 -> pool,
   else fast ADD path" happened to agree with goken by coincidence;
   a second global at a nonzero offset exposed that goken's real
   condition is "immrot(offset-BIG) succeeds and isn't exactly 0",
   which for any small data segment is essentially never true (BIG
   is meant for symbols positioned near it in a *large* segment) --
   so goken pool-loads both, while xix's old code fast-pathed the
   nonzero one. Verified: full baseline (5 fixtures) + make test
   (134/134) still pass after.
** DONE add tests/linker/arm_diff/kitchen_sink.s (hand-written, not from goken)
   Combines ADD/SUB/MUL/SLL, a BL/RET local call, address-of-global,
   and write/exit in one straight-line program -- broader coverage
   per fixture. Writing it caught a real bug (see immrot/BIG entry
   below), then an unrelated real *finding* once fixed: an earlier
   version used CMP+BEQ to pick between two exit messages, and goken
   turned out to duplicate the short exit epilogue (mov r7,#1; svc
   #0; pop {pc}) into *both* branches instead of having one branch
   to a shared tail -- i.e. goken doesn't always emit the branch our
   codegen would for a "B labelname" whose target sits past another
   block; something in 5a/5l's asm/noop layer decides to duplicate
   short tails instead. Real, but distinct from anything in our case
   list -- not chased now. Rewrote the fixture branch-free instead of
   chasing it. Worth its own investigation later if CMP/Bxx-heavy
   fixtures are added.
** DONE extend corpus: tests/linker/arm_diff/{exit_linux_arm,addr_arm,call_arm}.s
   Moved hello_linux.s from tests/linker/ into tests/linker/arm_diff/
   (a subfolder, not its own top-level dir: these fixtures exercise
   assembler codegen + basic exec layout more than linking proper --
   one object file each, no cross-object resolution -- but it's
   still o5a+o5l's combined output being compared, and a sibling
   tests/linker/mips_diff/ is expected once MIPS gets this treatment
   too). Added 3 more from goken's own tests/s/{exit,variants}/: exit_linux.s
   (minimal exit(42), no data section -- caught the elfstrtab 1-byte
   bug above), addr.s ($sym(SB) address-of-global + pointer-valued
   DATA), call.s (BL to a symbol, two TEXT procs). All 4: PASS
   byte-identical + PASS same qemu-arm exit code. Also fixed
   diff-arm.sh: `set -e` was aborting the script on a fixture's
   intentional non-zero exit code (exit_linux.s exits 42).
** DONE tests/linker/arm_diff/hello_linux.s: cmp -l clean vs goken
   PASS: byte-identical (4112 bytes both sides). Last 6 bytes closed
   in Layout5.ml: implemented goken's checkpool()'s "true end of
   program" flush trigger (`if(p->link==P) flushpool(p,true)`) --
   inserting a synthetic self-branch ("B .", an unreachable trap)
   right before the flushed literal pool, matching goken's always-
   guard-against-fallthrough behavior even though it's dead code
   here. The self-loop falls out naturally from gbranch_static
   reading the branch's own .branch field. Heavily commented in
   Layout5.ml since the literal-pool mechanism itself (not just this
   fix) wasn't well understood going in; added two permanent
   Logs.debug traces (pool-entry-added, pool-flushed) for next time.
   Mid-function early-flush (pool > ~4KB or LDR offset overflow) is
   NOT implemented -- pools must stay small; a real limit, not just
   a decision to revisit later. Verified full `./test-arm.sh` +
   `make test` (134/134) after.
** TODO literal-pool value deduplication (goken's addpool() reuses an
   existing pool word when a new entry's constant/address value
   exactly matches one already pending, instead of adding a
   duplicate) -- NOT ported. Discovered while verifying case 30/31
   (see below): a fixture using the same large offset twice diverges
   in pool *layout* from goken even though the actual load/store
   instructions it generates are correct. Low urgency (no test
   currently needs it -- tests/linker/arm_diff/longoff_case30_31.s is
   deliberately written to avoid triggering it), but real; will
   resurface for any fixture that repeats a big constant/address.
** DONE tests/linker/arm_diff/hello_linux.s: runs correctly under qemu-arm
   Root cause found and fixed: linker/Flags.ml's kencc_compatible
   defaulted to true, which sets the MOV instruction's Rn field to the
   destination register instead of 0 (Codegen5.ml:441). goken always
   encodes Rn=0 for MOV; qemu-arm's decoder actually SIGILLs on the
   Rn=rt encoding (verified by hex-patching just those 3 bytes in an
   otherwise-crashing binary -- that alone fixed it). Fixed by
   flipping the default to false. One-line fix, no case/logic touched.

* Phase 1 -- codegen.c case backlog (goken/linkers/5l/codegen.c)
Cases with a matching (* case N: *) comment already in Codegen5.ml:
** DONE case 1  op R,[R],R
** DONE case 2  movbu $I,[R],R
** DONE case 3  add R<<[IR],[R],R
** DONE case 4  add $I,[R],R (address-of-global fast path, SB-relative)
** DONE case 5  bra s (both B/BL/AL and Bxx/conditional -- same optab
   case, only p->scond differs, e.g. optab.c's ABEQ row)
** DONE case 6  b ,O(R) -> add $O,R,PC
** DONE case 7  bl ,O(R) -> mov PC,link; add $O,R,PC
** DONE case 8  sll $c,[R],R -> mov (R<<$c),R
** DONE case 9  sll R,[R],R -> mov (R<<R),R
** DONE case 10 swi [$con]
** DONE case 14 movb/movh/movhu R,R -> SLL/SRA/SRL (NOT movbu, that's
   case 58 -- goken's own case-14 comment text is slightly imprecise)
** DONE case 15 mul r,[r,]r
** DONE case 20 mov/movb/movbu R,O(R) (short/12-bit offset store)
** DONE case 21 mov/movbu O(R),R (short/12-bit offset load; Byte U
   only -- signed byte/halfword load is case 22, not this)
** DONE case 41 rfe -> movm.s.w.u 0(r13),[r15]
** DONE case 58 movbu R,R -> AND $0xff, R, R
** DONE case 13 op $lcon, [R], R -- generic Arith (AND/ORR/EOR/ADD/
   SUB/BIC/ADC/SBC/RSB/RSC/MVN/MOV, aliased via goken's buildop() to
   the same oprange as AADD/AMVN/ACMP) with a too-big immediate:
   load it into REGTMP via the literal pool first, then apply the
   real op using REGTMP as the "from" register (size=8: LDR + OP).
   Added tests/linker/arm_diff/lcon_case13.s (ADD $0x12345678,R0,R1).
   PASS byte-identical + PASS same qemu-arm exit code first try.
** DONE case 4/34 address-of-local/param ($lacon) -- mirrors the
   address-of-global case 4/12 split, just SP-relative instead of
   SB-relative: fast path (fits immrot) is a single ADD, slow path
   is REGTMP via the literal pool + ADD. Reused
   base_and_offset_of_indirect (feeding it `Entity entity` for the
   Local/Param case) instead of duplicating its offset arithmetic.
   Caught two real, previously unverified bugs -- nothing before
   this exercised Entity(Local)/Entity(Param) at all, every earlier
   fixture used raw Indirect(reg,off) syntax like `4(R13)` instead:
   - base_and_offset_of_indirect's "+4 for the caller/RLINK-slot"
     adjustment was on the wrong branch: Param (this codebase's name
     for the SP-mapped token, per Parser_asm5.mly's `pointer` rule)
     had it, Local (the FP-mapped token) didn't; should be the other
     way. Confirmed directly against goken with plain memory access
     (`MOVW x-8(FP),R1` / `MOVW x+8(SP),R1`, no $) before touching
     any $lacon code, to isolate this from the $lacon-specific issue
     below.
   - goken's aclass() (span.c) decides RACON-vs-LACON (fits-immrot
     fast path vs literal-pool slow path) using the offset *before*
     Rewrite5.rewrite's own "+4 for RLINK-save" adjustment to
     autosize -- a value no longer available by the time Codegen5
     runs (Rewrite5 bakes it permanently into the TEXT node's frame
     size: `n.instr <- T.TEXT (..., size+4)`). Confirmed empirically:
     frame=$8192, off=-8 gives a fully-adjusted offset of 8192
     (fits immrot), but goken still emits the slow/pool form, because
     ITS classification value is 8192-8=8184 (doesn't fit). Fixed by
     reconstructing the pre-adjustment offset (env.autosize - 4 + off)
     purely for the fits-check, while still encoding the real,
     fully-adjusted offset in either branch. This is not a rare edge
     case -- any real program combining a round frame size (4096,
     8192, ...) with a small local/param offset can hit it, so it
     was worth chasing rather than dodging with a "safe" fixture.
   Added tests/linker/arm_diff/lacon_case4_12.s (fast-path Local, fast-path
   Param, and a second slow-path instance for good measure). PASS
   byte-identical + PASS same qemu-arm exit code.
** DONE case 30/31 mov/movb/movbu R,L(R) / L(R),R -- long (>12-bit)
   stack/SB-relative offset: load it into REGTMP via the literal
   pool, then a register-offset LDR/STR (gmem's existing
   Either.Right path). Caught two real, previously-latent bugs while
   verifying against goken:
   - gmem's Either.Right (register-offset) branch never set the U
     (add) bit, defaulting to SUB instead of ADD -- goken's
     olr()/olrr() always ADD when passed a register number (always
     >= 0), since olrr reuses olr's "v>=0 -> U bit" check by passing
     the register number itself as that check's input. Nothing
     before case 30/31 ever exercised gmem's register-offset path.
   - Layout5.ml's literal_pools is a stack (LIFO push) but was
     flushed without reversing, so a pool with 2+ simultaneous
     entries came out in reverse insertion order vs goken's FIFO
     addpool()/flushpool(). Every earlier fixture only ever had one
     pending pool entry per flush, so this never showed up before.
   Also surfaced (see the separate "literal-pool value
   deduplication" TODO above): goken dedups repeated pool values,
   we don't yet -- tests/linker/arm_diff/longoff_case30_31.s deliberately
   uses each offset only once to isolate case 30/31's own
   correctness from that gap.
   PASS byte-identical + PASS same qemu-arm exit code.
** DONE case 11 word -- already handled generically by the shared
   Codegen.default_rules (used by ARM/MIPS/RISC-V alike): AWORD is
   never hand-written (it's what 5a itself generates for a
   literal-pool entry), so this is verified indirectly by every
   fixture whose codegen falls back to a literal pool (bigimm_case12.s
   for case 12, lcon_case13.s for case 13), not by a dedicated fixture.
** DONE case 54 floating point arith (ArithF: ADDF/SUBF/MULF/DIVF/
   ADDD/SUBD/MULD/DIVD, plus CmpF: CMPF/CMPD which share this case
   in codegen.c via `if(p->to.type==D_NONE) rt=0` -- kept as its own
   OCaml arm since Ast_asm5.CmpF is already a separate constructor).
   This is goken's legacy ARM 7500 FP (coprocessor 1) encoding --
   defunct hardware (no chip has implemented it since the
   ARM7500FE era), but it's what goken emits BY DEFAULT
   (`vfp = debug['f']`, off unless -f is passed; VFP -- cases 74-76
   -- is the opt-in one, same "disabled unless a flag is passed"
   pattern as armv4/cases 22/23/32/33, just the opposite default
   polarity). Float immediates are limited to goken's chipfloats[]
   (float.c): exactly {0,1,2,3,4,5,0.5,10} -- anything else is a
   genuine assembler error here, not goken's own silent
   `diag(); rf=0` fallback. Added tests/linker/arm_diff/fpa_case54.s.
   PASS byte-identical; confirmed empirically that qemu-arm has no
   FPA coprocessor at all to trap/emulate it -- both sides SIGILL
   identically (rc=132) on the very first FPA instruction, which is
   itself the matching signal (same crash, not a different one),
   same reasoning as swp_case40.s's deliberately-unmapped test address.
** DONE case 40 swp oreg,reg,reg -- ARM's atomic exchange (SWPW/
   SWPBU), deprecated since ARMv6 in favor of LDREX/STREX but still
   real (goken's own runtime uses it for spinlocks, e.g.
   GO/pkg/runtime/arm/cas5.s: `SWPW (R4), R3`). The AST shape
   (Ast_asm5.SWAP) and grammar (Parser_asm5.mly) already existed but
   the SWPW/SWPBU keyword-table entries were never wired up in
   Parse_asm5.ml -- added those two lines. The 2-operand grammar form
   (`SWPW (R4), R3`) is the classic atomic-exchange-in-place idiom:
   the same register is both the new value written (Rm) and the
   destination that receives the old value (Rd); the 3-operand form
   is implemented too but not independently verified against a real
   fixture. Added tests/linker/arm_diff/swp_case40.s.
** MAJOR FIND, FIXED: immrot() was subtly wrong for some values --
   goken's own `immrot(ulong v)` (5l/span.c) is, relative to true ARM
   rotated-immediate semantics, buggy on a 64-bit host: `ulong` is
   64-bit unsigned here (typedef in include/core/types.h), but the
   rotation trick (`v = (v<<2)|(v>>30)` up to 16 times) was written
   assuming 32-bit wraparound. On a 64-bit `ulong`, bits pushed past
   bit 63 are simply lost and `v>>30` starts pulling in bits that
   never actually wrapped around from a genuine 32-bit rotation --
   so for some values that ARE valid ARM rotated immediates (e.g.
   0x9000 = 0x90 rotated), goken's own immrot() returns 0 (not
   encodable), and it takes the literal-pool slow path (case 12/34)
   instead of a single MOV/ADD (case 2/4). Since the whole point of
   this port is byte-for-byte matching THIS actual goken binary (not
   "more correct" ARM codegen), Codegen5.ml's immrot needed to
   replicate that exact 64-bit non-wrapping computation, not the
   mathematically-correct 32-bit version it had before -- rewritten
   using Int64 (OCaml's native int can't safely hold the needed
   64-bit unsigned patterns). This was previously undetected because
   every earlier fixture's constants either fit directly (0-255, way
   below where the two versions could ever disagree) or were clearly
   too large either way (like 0x12345678); tests/linker/arm_diff/
   swp_case40.s's `MOVW $0x9000, R4` is what exposed it. Re-verified
   the entire existing corpus (all fixtures using case 2/4/12/13/34)
   still passes byte-identical after the fix -- nothing had
   coincidentally depended on the old, wrong 32-bit behavior.
** DONE case 72/73 movh/movhu R,L(R) -> strh / movb/movh/movhu L(R),R
   -> ldrsb/ldrsh/ldrh -- long (>8-bit) offset halfword/signed-byte
   store/load, same REGTMP-via-literal-pool pattern as case 30/31,
   using ghalfword's new register-offset form (extended from
   immediate-only: bit22=0, offset register directly in bits[3:0],
   no magnitude limit -- ported from goken's oshrr/olhrr, which XOR
   olhr/oshr's immediate-mode bit22 off). Added
   tests/linker/arm_diff/halflong_case72_73.s (each offset used exactly
   once, same literal-pool-dedup-avoidance reasoning as
   longoff_case30_31.s). PASS byte-identical + PASS same qemu-arm exit
   code.
** DONE case 70 movh/movhu R,O(R) -> strh (real ARMv4T STRH, ghalfword
   helper)
** DONE case 71 movb/movh/movhu O(R),R -> ldrsb/ldrsh/ldrh (real
   ARMv4T LDRSB/LDRSH/LDRH). Added ghalfword (ARM's distinct
   Load/Store-Halfword-and-Signed-Byte bit layout, split 8-bit
   immediate, SH-bit variant selector) -- caught a real bug while
   verifying against goken: the STORE side (case 70/oshr) never looks
   at signedness at all (there's only one STRH), only the LOAD side
   (case 71/olhr's XOR based on p->as) does; ghalfword's first draft
   wrongly varied the SH bits by signedness for STORE too, encoding
   STRH as a bogus STRD-shaped opcode -- caught by a 1-byte cmp -l
   diff, fixed, now genuinely byte-identical (not just "close").
   Added tests/linker/arm_diff/halfword_case70_71.s. PASS byte-identical +
   PASS same qemu-arm exit code.
** SKIP  cases 22/23/32/33 (movb/movh/movhu via byte-split-and-shift,
   the pre-ARMv4T fallback for O(R) and L(R) offsets) -- confirmed
   dead in practice: goken's buildop() only disables the V4-flagged
   optab rows (70-73, real STRH/LDRH/LDRSB/LDRSH) when `armv4` is
   false, and `armv4 = !debug['h']` (5l/span.c) -- true by default,
   so 70-73 always win over 22/23/32/33 unless goken is invoked with
   -h, which this harness never does. Porting 22/23/32/33 would be
   porting code no test here could ever exercise or verify.
** DONE case 74/75 vfp floating point arith/compare -- turns out this
   IS important in practice, not just a "-h"-style dead-code corner:
   real ARMv6+ hardware (e.g. Raspberry Pi 1's ARM1176JZF-S) and
   principia-softwarica's own kernel/userland both target VFP, not
   FPA -- FPA is defunct hardware (see case 54's DONE entry), so any
   float-using ARM program built with goken's *default* settings
   would be non-functional on that real hardware, same failure mode
   as fpa_case54.s's SIGILL under qemu-arm. `vfp = debug['f']`
   (5l/span.c) is OFF by default (opposite polarity of the armv4
   story), so this needed a new xix-side flag (`-f`, Flags.vfp,
   ARM-only) mirroring goken's, plus scripts/diff-arm.sh and
   test-arm.sh threading an optional extra-flags field through to
   pass -f identically to both 5l and o5l.
   ArithF's dyadic path (ADD_/SUB_/MUL_/DIV_) now dispatches on
   !Flags.vfp between gop_arithf (FPA) and gop_arithf_vfp (VFP,
   opvfprrr) -- VFP doesn't support float immediates at all (goken
   diag()s on D_FCONST there) and our AST's arithf_opcode has no
   MOVF/MOVD/MOVFD/MOVDF (case 74's monadic move/precision-
   conversion ops), so only the dyadic path was ever reachable here
   anyway. CmpF likewise dispatches -- VFP's compare is a genuinely
   different shape from FPA's: it needs a *second* fixed instruction
   ("MRS APSR_nzcv, FPSCR", gop_cmpf_vfp_mrs) to move the comparison
   result into the ARM CPSR where Bxx reads flags from (size=8 vs
   FPA's size=4), and the field layout for the 2nd operand differs
   (bit12 vs FPA's bit16).
   Added tests/linker/arm_diff/vfp_case74_75.s: register-register ADDF
   starting from the zero-initialized register state (Linux's ELF
   loader zeroes VFP registers too) + CMPF + BEQ. PASS
   byte-identical on the first try, AND -- unlike fpa_case54.s --
   PASS genuinely correct execution under qemu-arm (rc=0, the
   CMPF/BEQ branch actually taken): VFP is real, still-relevant
   hardware, so this is functionally verified, not just
   encoding-parity like the FPA/SWP fixtures.
** DONE case 55/76 floating point fix and float (int<->float
   conversion, MOVWF/MOVFW/MOVWD/MOVDW) -- added the AST/grammar
   work this needed (Ast_asm5.MOVWF/MOVFW, Parser_asm5.mly/
   Parse_asm5.ml keyword table, Meta_ast_asm5.ml's hand-maintained
   vof_instr, Rewrite5.ml's exhaustive match) since without this a
   VFP/FPA program could compute but never get a real integer value
   into or out of a float register -- genuinely useful now, not just
   a register-shuffling demo. IMPORTANT: adding AST constructors
   bumped Object_file.version (8->9) per Ast_asm5.ml's own header
   warning -- a stale bin_dune copy built against the old count of
   constructors segfaulted (not a catchable OCaml exception) on
   every fixture, not just the new ones, until rebuilt; this is the
   same bin_dune staleness gotcha as always, just with an unusually
   scary-looking symptom this time.
   FPA (case 55, gop_fixfloat): direction (dirbit at bit20: 0=to
   float, 1=to int) and D-precision (bit7) vary; register placement
   differs by direction (int reg @ bit12/freg @ bit16 for ToFloat,
   freg @ bit0/int reg @ bit12 for ToInt).
   VFP (case 76) is a genuinely different shape: converting between
   a core register and a VFP register needs an extra VMOV to shuttle
   the raw bit pattern in/out first (VFP's convert instructions only
   operate within the VFP register file) -- 2 instructions each
   direction, using FREGTMP (F15/D15, matching int-side REGTMP) as
   the scratch register for the ToInt direction. Got the encoding
   wrong twice on the first attempt (caught by testing against goken
   directly, not by manual verification): missed the `0xe<<24` base
   bits on the two opvfprrr-formula instructions (the VMOV literals
   already have it baked in, but the *convert* instructions don't --
   easy to reuse the wrong "already has 0xe24" assumption from the
   VMOV pattern above them), and mis-derived which hex digit encodes
   VMOV's direction bit (bits[23:20], a genuinely separate,
   non-overlapping field -- not bits[19:16] as first computed, which
   is actually the register slot).
   Added tests/linker/arm_diff/fixfloat_fpa_case55.s and fixfloat_vfp_case76.s
   (both: 42 -> float reg -> back -> exit(42)). PASS byte-identical
   AND PASS genuine functional round-trip under qemu-arm (rc=42 both
   sides, both FPA and VFP) -- FPA's arithmetic SIGILLs
   (fpa_case54.s) but apparently qemu does implement these two
   particular legacy opcodes.
** DONE case 12 movw $lcon, reg -- both the address-of-global slow
   path (MOVW $L(SB),RT -> LDR from literal pool) and the plain
   too-big-immediate case (MOVW $0x12345678,R -> LDR from literal
   pool, reusing Ast_asm.Int through the same PoolOperand mechanism,
   already generically handled by Codegen.default_rules's WORD case).
   Added tests/linker/arm_diff/bigimm_case12.s. PASS byte-identical +
   PASS same qemu-arm exit code (120, from the truncated-to-byte
   0x12345678 status value).
** SKIP  case 16 div r,[r,]r -- goken itself just emits an invalid/
   trap opcode here (o1 = 0xf<<28, the ARM "never" condition with a
   bare invalid encoding; ARMv5/v6 has no hardware integer divide).
   Porting this would mean porting a trap, not real functionality;
   low value. Real div/mod on this target is a compiler (occ)
   concern -- runtime library calls -- which is out of scope (see
   "Compiler is out of scope" in the plan doc).
** N/A   case 17 is AMULL (64-bit long multiply, register-pair
   result), NOT mod/div as this file previously guessed -- optab.c
   has no AMOD entry at all. Moved off the div/mod story; tracked
   below with the other unported arithmetic cases.

Not yet in Codegen5.ml at all:
** DONE case 17 mull (64-bit long multiply, register-pair result) --
   real, reachable syntax: "MULL cond R1,R2,(HI,LO)" (a.y's `regreg:
   '(' regi ',' regi ')'`), all 4 sign/accumulate variants
   (MULL/MULLU/MULAL/MULALU) sharing one new instr constructor
   (`MULL of A.sign * bool * reg * reg * reg * reg`) since goken's
   own oprrr() encodes the 4 mnemonics as a 2-bit sub-field (sign,
   accumulate) rather than 4 unrelated opcodes. New `gmull_opcode`
   helper for that bit packing -- caught a real bug while testing:
   the first version had the sign and accumulate bits swapped
   (MULL's own encoding came out identical to MULALU's), found by
   mull_case17.s and confirmed against goken directly.
   Added mull_case17.s (all 4 variants computing the same product
   into 4 different register pairs, MULAL/MULALU accumulating onto
   pre-zeroed registers so all 4 results should match, summed into
   the exit code -- exactly the kind of test that would have caught
   the bit-swap bug above). Two different multiplicands ($100000/
   $100001, not the same constant twice) to avoid the already-known
   literal-pool-deduplication gap, same reasoning as
   longoff_case30_31.s. PASS byte-identical + PASS same qemu-arm
   exit code. Verified: full ARM/MIPS/RISC-V baselines and make test
   (134/134) all still pass.
** DONE case 35 mov PSR,R (MRS) / case 36 mov R,PSR (MSR) / case 37
   mov $con,PSR (MSR) -- real, reachable via the same "MOVW"
   mnemonic/gen mechanism as FP[CS]R (case 56/57): new `psrreg`
   (CPSR/SPSR, goken's D_PSR) and `PSRImsr of psrreg` on
   `mov_operand`. New Codegen5.ml helpers `gpsr_read` (MRS) and
   `gpsr_write_base` (MSR, shared by both the register and immediate
   source forms; case 37's immediate goes through the existing
   `immrot` helper, erroring loudly like goken's own diag() if the
   constant isn't immrot-encodable rather than silently emitting
   wrong bytes -- no literal-pool fallback).
   Scoped limitation: the ".F" (flags-only write) suffix isn't
   wired -- xix's grammar has no dot-suffix-flag parsing mechanism
   at all yet (goken unifies .S/.P/.W/.U/.F/etc into one generic
   `scond` bitmask via a shared grammar rule xix never ported; even
   the already-existing arith_cond/move_cond fields, e.g. Arith's
   own ".S", are hardcoded to `None` in every Parser_asm5.mly
   production, never actually parsed from real .s text). Building
   that subsystem from scratch for one flag bit wasn't worth it;
   this always emits the "full PSR write" encoding, which is still
   a real, independently-testable instruction shape on its own.
   Added psr_move_case35_36_37.s (CPSR round-trip: read, write from
   register, write from immediate, read again -- exit code depends
   on the actual CPSR bits qemu-arm's user-mode emulation preserves).
   PASS byte-identical (334 bytes both sides) + PASS same qemu-arm
   exit code. SPSR verified ad hoc (not its own fixture, same
   reasoning as fcr_move_case56_57.s's FPCR aside): byte-identical
   against goken and traps identically as "Illegal instruction" on
   both sides (no SPSR access in user mode) -- confirms the shared
   encoding but adds no further coverage.
** TODO case 38 movm $con,oreg -> stm
** TODO case 39 movm oreg,$con -> ldm
   (cases 38/39 likewise need a MOVM AST constructor first, plus
   parsing the .IA/.DB/.FD/.ED-style addressing-mode suffixes and a
   register-list syntax `[R0-R3,R14]` -- Ast_asm5.ml's own prelude
   TODO already flags "MOVM (and his special bits .IA/...)" as
   unrepresented. Same category of work as 35-37: assembler
   grammar/AST first, Codegen5.ml second.)
** DONE case 50 floating point store / case 51 floating point load /
   case 52 floating point store, long offset UGLY / case 53 floating
   point load, long offset UGLY -- MOVF/MOVD to/from a plain register
   +offset address, the float analogue of case 20/21/30/31 (int
   store/load). Unlike case 59-63/64-69, this one IS real, reachable
   .s syntax (optab.c's C_FEXT/C_FAUTO/C_FOREG/C_LEXT/C_LAUTO/C_LOREG
   rows, not C_ADDR's DLM-only or C_SHIFT's peephole-only path), so
   it's byte-tested against 5a/5l like every ordinary case.
   Required new AST, kept ARM-local rather than extending the shared
   Ast_asm.move_size (which has no Float case and is used by every
   arch): added `FImsr of freg` to `mov_operand` (the register side;
   the memory side still reuses Indirect/Entity and
   base_and_offset_of_indirect as-is, since float load/store still
   addresses memory through a plain *integer* base register) and a
   new top-level `MOVEF of A.floatp_precision * mov_operand *
   mov_operand` instr constructor (mirroring MOVE's shape but keyed
   on floatp_precision instead of move_size). Object_file.version
   bumped 17->18. Grammar: new TMOVF token, "MOVF"/"MOVD" keywords,
   and `gen` (already producing mov_operand) grew a bare `freg`
   alternative.
   New encoding helpers gfsr/gfsr_vfp (ofsr()/ovfpmem() in
   codegen.c), dispatched via the same `!Flags.vfp` convention as
   ArithF/CmpF/MOVWF/MOVFW. New fimmoffset (the short-offset range:
   a signed, word-aligned offset with magnitude < 1024, i.e. gfsr's
   8-bit word-count field) alongside the existing immoffset. The
   long-offset case (52/53) needs an explicit `ADD REGTMP,Rbase,
   REGTMP` after loading the offset from the literal pool, unlike
   int case 30/31's gmem-register-offset shortcut -- FPA/VFP
   load/store has no register-offset addressing mode at all, only a
   fixed-size immediate, so the full address must be materialized in
   REGTMP first.
   Added float_mem_case50_51.s / float_mem_vfp_case50_51.s
   (short-offset store+load round-trip through MOVWF/MOVFW into the
   exit code) and float_mem_longoff_case52_53.s /
   float_mem_vfp_longoff_case52_53.s (long-offset, mirroring
   longoff_case30_31.s's own two-different-offsets workaround for the
   already-known literal-pool-deduplication gap -- confirmed hitting
   that exact 2-byte diff with a same-offset round-trip version
   before rewriting it this way). PASS byte-identical + PASS same
   qemu-arm exit code, all four (FPA/VFP x short/long-offset).
   Verified: full ARM/MIPS/RISC-V baselines and make test (134/134)
   all still pass.
** DONE case 56 move to FP[CS]R / case 57 move from FP[CS]R -- FPSR/
   FPCR moves. Unlike case 50-53, goken dispatches these through the
   exact same "MOVW" mnemonic/gen mechanism as an ordinary int move
   (a.y's `gen: ... | LFCR`), not a distinct mnemonic -- so this is a
   new mov_operand alternative (`FCRImsr of fcrreg`, `fcrreg = FPSR |
   FPCR`) handled inside the existing MOVE(Word,...) match arms
   (case 20's store and case 21's load), not a new instr constructor.
   New `gfcr` helper (goken's fixed coprocessor-1 register-transfer
   encoding; the L bit, read vs write, added by the caller like
   gfsr/gfsr_vfp). No VFP variant exists for this in goken (no case
   in the 70s range for it), so no `!Flags.vfp` dispatch needed here.
   Added fcr_move_case56_57.s (FPSR write+read round-trip into the
   exit code -- FPSR is genuinely accessible from user mode under
   qemu-arm). FPCR (same encoding, fcr_val=2) verified via an ad-hoc,
   non-committed test instead: byte-identical against goken, and
   traps as "Illegal instruction" identically on both sides (not
   emulated by qemu-arm) -- confirms gfcr's encoding but adds no
   further coverage, so not added as its own fixture. PASS
   byte-identical + PASS same qemu-arm exit code. Verified: full
   ARM/MIPS/RISC-V baselines and make test (134/134) all still pass.
** TODO (investigated, deliberately not implemented) case 59 movw/bu
   R<<I(R),R -> ldr indexed / case 60 movb R(R),R -> ldrsb indexed /
   case 61 movw/b/bu R,R<<[IR](R) -> str indexed -- same story as
   case 62/63 (see that entry): goken's 5a grammar has no rule
   combining a shift operand with a base register, so this can't be
   spelled in real .s text; compilers/5c/peep.c confirms it's a
   compiler peephole (fusing a separate shift + load/store into one
   D_SHIFT-operand instruction) rather than something 5a ever parses.
   No way to build a byte-identical fixture against 5a/5l. Documented
   in Codegen5.ml instead of repeating the CASE/BCASE detour. Also
   unlikely to ever be needed: xix's own future C compiler (occ) is
   planned to use c-- as a backend, which does its own peepholing and
   emits plain, unfused assembly, not this exact shape.
** TODO (investigated, deliberately not implemented) case 62 case R
   -> movw R<<2(PC),PC / case 63 bcase -- switch-statement jump
   table. Confirmed real (unlike MIPS's case 45/46, see
   mips_port.md): goken's 5c *does* emit ACASE/ABCASE for a
   dense-enough switch (compilers/5c/swt.c's `swit2`). But goken's 5a
   (this port's actual, permanent target) has no grammar rule for
   either mnemonic -- only the separate, explicitly out-of-scope
   5ak/5lk lineage can parse "CASE"/"BCASE" as text at all -- so
   there is no way to ever build a byte-identical fixture against
   5a/5l for this case.
   A prototype was built and worked (encoding confirmed bit-for-bit
   against linkers/5l/codegen.c and cross-checked once against
   goken's 5ak/5lk output; a hand-run fixture dispatched correctly
   under qemu-arm with xix's own o5a/o5l) by adding `Case`/`BCase`
   *generically* to Ast_asm.ml's shared virtual_instr, parsed via a
   shared CASE/BCASE keyword duplicated in every arch's grammar, with
   only Rewrite5.ml/Codegen5.ml doing a real lowering. Reverted rather
   than kept: it required loosening two previously-clean, shared
   invariants that held across every arch -- assembler/
   Resolve_labels.ml's label-resolution pass never looking inside
   `Virtual` nodes at all, and Rewrite{5,v,i}.ml's step1 blanket
   "branch should not be set on virtual instr" check (BCase is the
   first Virtual construct ever reachable from real .s source
   carrying an unresolved branch target, which is what exposed both).
   Not worth that cost for a feature with no current consumer
   (nothing in xix emits switch-statement code yet) and no possible
   differential test against the actual target. Revisit only if a
   real driver shows up (e.g. a future xix C compiler backend wanting
   jump-table codegen) -- at that point the actual requirements would
   be concrete instead of guessed at, and might justify the shared-
   invariant changes on their own merits.
** TODO case 64 mov/movb/movbu R,addr
** TODO case 65 mov/movbu addr,R
** TODO case 66 movh/movhu/movb addr,R
** TODO case 67 movh/movhu R,addr -> sb, sb
** TODO case 68 floating point store -> ADDR
** TODO case 69 floating point load <- ADDR
