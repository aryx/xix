; used to be in Common.ml but it might depends on lib such as Str that
; requires C code so simpler to keep Common pure ocaml and move the
; regexp stuff in a separate lib.

(library
 ;(public_name profiling)
 (name xix_regexp)
 (wrapped false)
 (libraries
   xix_commons
 )
)
