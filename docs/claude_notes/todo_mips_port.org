# -*- org -*-
# Progress tracker for docs/claude_notes/notes_mips_port_plan.txt.
# See that file for rationale/design; this file is just checklist state.

* Phase 0 -- harness
** DONE diff harness script (assemble+link both sides, cmp -l, qemu-mips)
   scripts/diff-mips.sh + test-mips.sh, mirrors the ARM ones. No -H
   flag needed (vl/ovl both default to ELF already). qemu-mips, not
   qemu-mipsel, since Mips is now Endian.Big.
** DONE fix a real, previously-flagged, endianness bug
   lib_core/commons/Arch.ml's Mips was Endian.Little (PS1-motivated);
   switched to Endian.Big to match goken and enable byte-equal
   testing (explicitly decided, dropping PS1 compat as a goal). That
   immediately reproduced the segfault the code had a standing TODO
   about ("if put Big here I get a segfault with ovl, why ???").
   Root cause: linker/Execgen.ml hardcoded Endian.Little.output_32
   for the TEXT section in both the A_out and Elf branches, so the
   ELF header claimed MSB while instruction words stayed LSB -- any
   big-endian consumer (qemu-mips) decoded garbage. Fixed by deriving
   the output function from Arch.endian_of_arch config.arch instead.
   Datagen.ml was already correctly parametrized by endian.
   Verified: ARM baseline (5 fixtures) still passes, make test still
   134/134, unaffected by this generalization.

* Phase 0.5 -- baseline corpus (constructs already ported)
** DONE tests/linker/mips_diff/hello_linux_mips.s: cmp -l clean + qemu-mips
   Copied from goken's tests/s/mini/hello_linux_mips.s (the simpler
   version, without xix's own tests/assembler/hello_linux_mips.s's
   extra nm/onm test block). PASS: byte-identical (4112 bytes both
   sides) on the first real run, once the endian bug above was fixed.
   Both sides print "Hello, world" and exit 0 under qemu-mips.
** DONE tests/linker/mips_diff/exit_linux_mips.s: cmp -l clean + qemu-mips
   Copied from goken's tests/s/exit/exit_linux_mips.s (no data
   section, same reasoning as exit_linux_arm.s for keeping a
   no-data fixture in the corpus). Caught a real bug: case 18's JMP
   (the expansion of a leaf RET) emitted just the jump, missing
   MIPS' mandatory branch-delay-slot instruction -- goken's noops()
   (vl/noop.c) always fills it, with `NOR R0,R0,R0` (funct 0x27)
   specifically, not the all-zero `SLL R0,R0,0` some other MIPS
   toolchains use as NOP (checked against goken's actual output
   byte-for-byte, first guess of a plain zero word was wrong).
   Fixed in Codegenv.ml (case 18 now emits 2 words, size=8). PASS:
   byte-identical (326 bytes) + matching qemu-mips exit code (42)
   after. Verified: MIPS hello-world fixture, ARM baseline (5
   fixtures), and make test (134/134) all still pass.
** DONE fix JAL's (case 11) missing delay slot -- functional bug, not just bytes
   Confirmed via tests/linker/mips_diff/call_mips.s (hand-written,
   no goken equivalent existed): missing delay-slot handling on JAL
   was a real correctness bug, not just a byte-cosmetic one. Without
   an explicit slot, the CALLER's next instruction (setting up the
   exit code, in this fixture) got silently consumed as the delay
   slot -- executing right after JAL but *before* the callee ran --
   and its effect was then clobbered by the callee's own register
   use, producing the wrong exit code (1 instead of 0) on xix while
   goken's build ran correctly. Fixed by giving JAL the same NOP
   treatment as case 18 (shared `nop` constant, size=8, hoisted the
   NOP definition out of case 18 into a shared let). Exit codes now
   match on both sides.
   Byte-equality is NOT fully reached for call_mips.s though (4 bytes
   still differ, offsets 180-184): goken's sched.c is a real
   instruction scheduler (dependency analysis via depend()/
   conflict()/offoverlap() in vl/sched.c) that hoists the callee's
   first instruction into the delay slot instead of a plain NOP, and
   duplicates it so it's still there for other entry paths. Decided
   NOT to port this: it's a genuine scheduler implementation, much
   bigger than any single case so far, for a payoff limited to a few
   bytes on call-adjacent code that's already functionally correct.
   call_mips.s is kept as a fixture but deliberately left out of
   test-mips.sh's CASES list for this reason (run it manually with
   scripts/diff-mips.sh to see the diff). Revisit only if a future
   fixture's byte-equality actually depends on it.
** DONE fix Codegenv.ml's offset_to_R30 (address-of-global) -- and why it was never started
   Mirrors the ARM immrot/BIG story (Codegen5.ml), but simpler in the
   end. offset_to_R30 had the same kind of stub ARM's offset_to_R12
   did ("LATER? x - BIG optimisation"), and the dispatch code had two
   dead-end `failwith "TODO: ..."` branches for any nonzero
   R30-relative offset -- confirmed by tests/linker/mips_diff/
   kitchen_sink_mips.s (hand-written, two GLOBLs at different
   offsets, same setup that found the ARM bug), which crashed ovl
   outright rather than just producing wrong bytes.
   Root-caused by reading goken's actual span.c aclass() (the D_ADDR/
   SDATA case): the fast R30-relative-ADD path's condition is
   `instoffset >= -BIG && instoffset < BIG && instoffset != 0`, and
   goken's own linkers/vl/l.h sets `BIG = 0` (an old value of 32766
   is left commented out right above it) -- collapsing the condition
   to `instoffset >= 0 && instoffset < 0`, which no integer ever
   satisfies. So this fast path is permanently dead in goken *itself*
   on MIPS, unlike ARM where BIG=4092 could occasionally still make
   it reachable for a large enough data segment. That's exactly why
   it was never started here: matching goken means never taking it,
   for any offset, so there was nothing to port beyond "always fall
   through to the absolute-constant (lu+or) load" -- which is what
   the fix now does unconditionally, replacing both failwith branches
   with a comment pointing at this explanation. Heavily commented in
   Codegenv.ml itself too, since neither the ARM nor the MIPS version
   of this mechanism was well understood going in.
   PASS: byte-identical (both addr_mips.s and kitchen_sink_mips.s,
   the latter on the very first run after the fix) + matching
   qemu-mips behavior. Verified: full MIPS + ARM baselines and
   make test (134/134) all still pass.

* Phase 1 -- asm.c case backlog (goken/linkers/vl/asm.c)
Cases with a matching (* case N: *) comment already in Codegenv.ml:
** DONE case 1  mov[v] r1,r2 ==> OR r1,r0,r2
** DONE case 3  mov $soreg, r ==> or/add $i,o,r
** DONE case 4  add $scon,[r1],r2
** DONE case 5  syscall
** DONE case 7  mov r, soreg ==> sw o(r)
** DONE case 8  mov soreg, r ==> lw o(r)
** DONE case 11 jmp lbra
** DONE case 18 jmp [r1],0(r2)
** DONE case 19 mov $lcon,r ==> lu+or
** DONE case 35 mov r,lext/luto/oreg ==> sw o(r)
** DONE case 36 mov lext/lauto/lreg,r ==> lw o(r30)

Not yet in Codegenv.ml at all:
** TODO case 0  pseudo ops
** TODO case 2  add/sub r1,[r2],r3
** TODO case 6  beq r1,[r2],sbra
** TODO case 9  asl r1,[r2],r3
** TODO case 10 add $con,[r1],r2 ==> mov $con,t; add t,[r1],r2
** TODO case 12 movbs r,r
** TODO case 13 movbu r,r
** TODO case 16 sll $c,[r1],r2
** TODO case 20 mov lohi,r
** TODO case 21 mov r,lohi
** TODO case 22 mul r1,r2
** TODO case 23 add $lcon,r1,r2 ==> lu+or+add
** TODO case 24 mov $ucon,,r ==> lu r
** TODO case 25 add/and $ucon,[r1],r2 ==> lu $con,t; add t,[r1],r2
** TODO case 26 mov $lsext/auto/oreg,,r2 ==> lu+or+add
** TODO case 27 mov [sl]ext/auto/oreg,fr ==> lwc1 o(r)
** TODO case 28 mov fr,[sl]ext/auto/oreg ==> swc1 o(r)
** TODO case 30 movw r,fr
** TODO case 31 movw fr,r
** TODO case 32 fadd fr1,[fr2],fr3
** TODO case 33 fabs fr1,fr3
** TODO case 34 mov $con,fr ==> or/add $i,r,r2
** TODO case 37 movw r,mr
** TODO case 38 movw mr,r
** TODO case 39 rfe ==> jmp+rfe
** TODO case 40 word
** TODO case 41 movw r,fcr
** TODO case 42 movw fcr,r
** TODO case 45 case r
** TODO case 46 bcase $con,lbra
** TODO case 47 sc r, soreg
** TODO case 48 ll soreg, r

* Notes for later
** Expect a BIG/immrot-equivalent story for address-of-global (R30 bias)
   hello_linux_mips.s's `MOVW $setR30(SB), R30` is MIPS' analogue of
   ARM's `MOVW $setR12(SB), R12`. When a fixture with multiple GLOBLs
   at nonzero offsets gets added (mirroring kitchen_sink.s), expect
   to need the MIPS equivalent of the ARM BIG-bias + immrot fix in
   Codegenv.ml's address-of-global path -- check for a similar
   "less: x - BIG" style stub there first.
