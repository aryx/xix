# -*- org -*-
# Progress tracker for docs/claude_notes/notes_mips_port_plan.txt.
# See that file for rationale/design; this file is just checklist state.

* Phase 0 -- harness
** TODO investigate: base_and_offset_of_entity's Param/Local +4 split
   (Codegenv.ml) has the *exact same shape* as a real, confirmed bug
   found in ARM's Codegen5.ml equivalent (base_and_offset_of_indirect):
   there, the "+4 for the caller/RLINK-slot adjustment" comment was
   attached to the wrong branch (Param had it, Local didn't; should
   have been the other way -- see docs/claude_notes/todo_arm_port.org's
   case 4/34 entry). This MIPS version reads identically (Param:
   `autosize + 4 + off`, Local: `autosize + off`, same comment
   wording) and was almost certainly copy-pasted from the same
   original source, so it may well have the identical bug -- but
   nothing has exercised Entity(Local)/Entity(Param) on MIPS yet (no
   fixture uses a named FP/SP-relative local), so this is unverified,
   not confirmed. Check directly against goken with plain memory
   access (`MOVW x-8(FP), R1` / `MOVW x+8(SP), R1` equivalents, no $)
   before trusting either branch, same methodology as the ARM fix.
** DONE diff harness script (assemble+link both sides, cmp -l, qemu-mips)
   scripts/diff-mips.sh + test-mips.sh, mirrors the ARM ones. No -H
   flag needed (vl/ovl both default to ELF already). qemu-mips, not
   qemu-mipsel, since Mips is now Endian.Big.
** DONE fix a real, previously-flagged, endianness bug
   lib_core/commons/Arch.ml's Mips was Endian.Little (PS1-motivated);
   switched to Endian.Big to match goken and enable byte-equal
   testing (explicitly decided, dropping PS1 compat as a goal). That
   immediately reproduced the segfault the code had a standing TODO
   about ("if put Big here I get a segfault with ovl, why ???").
   Root cause: linker/Execgen.ml hardcoded Endian.Little.output_32
   for the TEXT section in both the A_out and Elf branches, so the
   ELF header claimed MSB while instruction words stayed LSB -- any
   big-endian consumer (qemu-mips) decoded garbage. Fixed by deriving
   the output function from Arch.endian_of_arch config.arch instead.
   Datagen.ml was already correctly parametrized by endian.
   Verified: ARM baseline (5 fixtures) still passes, make test still
   134/134, unaffected by this generalization.

* Phase 0.5 -- baseline corpus (constructs already ported)
** DONE tests/linker/mips_diff/hello_linux_mips.s: cmp -l clean + qemu-mips
   Copied from goken's tests/s/mini/hello_linux_mips.s (the simpler
   version, without xix's own tests/assembler/hello_linux_mips.s's
   extra nm/onm test block). PASS: byte-identical (4112 bytes both
   sides) on the first real run, once the endian bug above was fixed.
   Both sides print "Hello, world" and exit 0 under qemu-mips.
** DONE tests/linker/mips_diff/exit_linux_mips.s: cmp -l clean + qemu-mips
   Copied from goken's tests/s/exit/exit_linux_mips.s (no data
   section, same reasoning as exit_linux_arm.s for keeping a
   no-data fixture in the corpus). Caught a real bug: case 18's JMP
   (the expansion of a leaf RET) emitted just the jump, missing
   MIPS' mandatory branch-delay-slot instruction -- goken's noops()
   (vl/noop.c) always fills it, with `NOR R0,R0,R0` (funct 0x27)
   specifically, not the all-zero `SLL R0,R0,0` some other MIPS
   toolchains use as NOP (checked against goken's actual output
   byte-for-byte, first guess of a plain zero word was wrong).
   Fixed in Codegenv.ml (case 18 now emits 2 words, size=8). PASS:
   byte-identical (326 bytes) + matching qemu-mips exit code (42)
   after. Verified: MIPS hello-world fixture, ARM baseline (5
   fixtures), and make test (134/134) all still pass.
** DONE fix JAL's (case 11) missing delay slot -- functional bug, not just bytes
   Confirmed via tests/linker/mips_diff/call_mips.s (hand-written,
   no goken equivalent existed): missing delay-slot handling on JAL
   was a real correctness bug, not just a byte-cosmetic one. Without
   an explicit slot, the CALLER's next instruction (setting up the
   exit code, in this fixture) got silently consumed as the delay
   slot -- executing right after JAL but *before* the callee ran --
   and its effect was then clobbered by the callee's own register
   use, producing the wrong exit code (1 instead of 0) on xix while
   goken's build ran correctly. Fixed by giving JAL the same NOP
   treatment as case 18 (shared `nop` constant, size=8, hoisted the
   NOP definition out of case 18 into a shared let). Exit codes now
   match on both sides.
   Byte-equality is NOT fully reached for call_mips.s though (4 bytes
   still differ, offsets 180-184): goken's sched.c is a real
   instruction scheduler (dependency analysis via depend()/
   conflict()/offoverlap() in vl/sched.c) that hoists the callee's
   first instruction into the delay slot instead of a plain NOP, and
   duplicates it so it's still there for other entry paths. Decided
   NOT to port this: it's a genuine scheduler implementation, much
   bigger than any single case so far, for a payoff limited to a few
   bytes on call-adjacent code that's already functionally correct.
   call_mips.s is kept as a fixture but deliberately left out of
   test-mips.sh's CASES list for this reason (run it manually with
   scripts/diff-mips.sh to see the diff). Revisit only if a future
   fixture's byte-equality actually depends on it.
** DONE fix Codegenv.ml's offset_to_R30 (address-of-global) -- and why it was never started
   Mirrors the ARM immrot/BIG story (Codegen5.ml), but simpler in the
   end. offset_to_R30 had the same kind of stub ARM's offset_to_R12
   did ("LATER? x - BIG optimisation"), and the dispatch code had two
   dead-end `failwith "TODO: ..."` branches for any nonzero
   R30-relative offset -- confirmed by tests/linker/mips_diff/
   kitchen_sink_mips.s (hand-written, two GLOBLs at different
   offsets, same setup that found the ARM bug), which crashed ovl
   outright rather than just producing wrong bytes.
   Root-caused by reading goken's actual span.c aclass() (the D_ADDR/
   SDATA case): the fast R30-relative-ADD path's condition is
   `instoffset >= -BIG && instoffset < BIG && instoffset != 0`, and
   goken's own linkers/vl/l.h sets `BIG = 0` (an old value of 32766
   is left commented out right above it) -- collapsing the condition
   to `instoffset >= 0 && instoffset < 0`, which no integer ever
   satisfies. So this fast path is permanently dead in goken *itself*
   on MIPS, unlike ARM where BIG=4092 could occasionally still make
   it reachable for a large enough data segment. That's exactly why
   it was never started here: matching goken means never taking it,
   for any offset, so there was nothing to port beyond "always fall
   through to the absolute-constant (lu+or) load" -- which is what
   the fix now does unconditionally, replacing both failwith branches
   with a comment pointing at this explanation. Heavily commented in
   Codegenv.ml itself too, since neither the ARM nor the MIPS version
   of this mechanism was well understood going in.
   PASS: byte-identical (both addr_mips.s and kitchen_sink_mips.s,
   the latter on the very first run after the fix) + matching
   qemu-mips behavior. Verified: full MIPS + ARM baselines and
   make test (134/134) all still pass.

* Phase 1 -- asm.c case backlog (goken/linkers/vl/asm.c)
Cases with a matching (* case N: *) comment already in Codegenv.ml:
** DONE case 1  mov[v] r1,r2 ==> OR r1,r0,r2
** DONE case 3  mov $soreg, r ==> or/add $i,o,r (fixed a real bug)
   constant_kind's range check was `i <= 0xffff` with no lower
   bound -- trivially true for ANY negative i in OCaml, so a
   genuinely out-of-range negative constant (needing case 19/24's
   lu-based expansion, not ported for a plain literal) would have
   silently gone through the direct-fit path instead of correctly
   failing loudly. Fixed to `i >= -0x8000 && i <= 0xffff`, matching
   goken's actual ZCON/SCON/ADDCON/ANDCON range (same boundary
   reasoning as the case 4/10 fix just below).
   Separately, also fixed which opcode case 3 emits: the old code
   always used ADDU regardless of i's sign, but goken picks ORI
   specifically for the ANDCON range (0x8000-0xffff) -- ADDU would
   sign-extend e.g. $0x8000 into -32768 instead of +32768. Confirmed
   against goken directly (`vl -a`): 0x7fff uses ADDIU, 0x8000/
   0xffff use ORI. New movw_imm_opcode helper picks OR vs ADD(W,U)
   accordingly.
   Added tests/linker/mips_diff/movw_andcon_mips.s (MOVW $32768 and
   $65535, the two ANDCON boundary values) -- no branches needed
   here (unlike case 4/10's immcon_mips.s), so this one is fully
   byte-identical, not just functionally correct. Added to
   test-mips.sh's CASES. Verified: full MIPS + ARM baselines and
   make test (134/134) all still pass.
** DONE case 5  syscall
** DONE case 7  mov r, soreg ==> sw o(r)
** DONE case 8  mov soreg, r ==> lw o(r)
** DONE case 11 jmp lbra
** DONE case 18 jmp [r1],0(r2)
** DONE case 19 mov $lcon,r ==> lu+or
** DONE case 35 mov r,lext/luto/oreg ==> sw o(r)
** DONE case 36 mov lext/lauto/lreg,r ==> lw o(r30)
** DONE case 2  add/sub r1,[r2],r3 -- generic register-register-
   register arith (ADD/SUB/AND, plus OR/XOR/SGT via goken's
   oprange-aliasing in span.c's buildop() equivalent, same pattern
   as ARM's buildop()). oprrr_arith_opcode already had all these
   opcodes encoded (used by case 1's OR trick already), so this was
   just wiring up the match arm -- explicitly excludes SLL/SRL/SRA
   even though oprrr_arith_opcode also handles those, since case 9
   ("asl r1,[r2],r3") uses a *different*, swapped operand order for
   shifts and isn't ported yet; reusing this arm for shifts would
   silently emit wrong bytes. Added tests/linker/mips_diff/
   case2_mips.s (ADD/SUB/AND chained into the exit code, avoiding
   plain register-to-register MOVW which isn't ported yet either).
   PASS byte-identical + PASS same qemu-mips exit code.
** DONE case 9  asl r1,[r2],r3 -- shift-by-register (SLL/SRL/SRA),
   same shape as case 2 but with the shift-amount register and the
   value-being-shifted register swapped in the encoding call
   (goken's `OP_RRR(oprrr(p->as), r, p->from.reg, p->to.reg)` vs
   case 2's `OP_RRR(oprrr(p->as), p->from.reg, r, p->to.reg)`) --
   exactly the gap flagged when case 2 was ported. Added
   tests/linker/mips_diff/case9_mips.s (SLL then SRL chained into
   the exit code: 1<<4=16, 16>>2=4). PASS byte-identical + PASS same
   qemu-mips exit code.
** DONE case 6  beq r1,[r2],sbra -- conditional branches: BEQ/BNE
   (2-register form, from AST's BEQ/BNE) and the whole
   ABGEZ/ABGEZAL/ABGTZ/ABLEZ/ABLTZ/ABLTZAL family (1-register-vs-zero,
   from AST's Bxx) -- goken's case 6 covers all of these uniformly
   with one formula (`OP_IRR(opirr(p->as), v, p->from.reg, p->reg)`,
   optab.c: all share oprange 6), split here into two match arms only
   because BEQ/BNE's middle register is a real optional operand
   (defaults to R0) while Bxx's "register" slot is always occupied by
   the BCOND sub-opcode bits instead (BCOND(x,y)=(x<<19)|(y<<16),
   selecting BGEZ/BGEZAL/BLTZ/BLTZAL within SP(0,1) -- see
   opirr_bxx_opcode). Needed a new op_irr_no_r3 helper for the Bxx
   arm: reusing plain op_irr with r3=0 would add a *second* Bits.t
   tuple at bit offset 16 on top of BCOND's own (y,16) contribution,
   which Bits.sanity_check_32 rejects (two entries at the same
   offset) even though the actual encoded value is identical (goken's
   C passes p->reg==NREG there, which OP_IRR's `&31` masks down to 0
   -- a no-op contribution, just not expressible as a *second*
   tuple in this representation).
   The branch offset itself needed a new helper (gbranch_offset),
   distinct from gbranch_static (JMP/JAL's *absolute* word address):
   case 6 is PC-relative (`v = (p->cond->pc - pc - 4) >> 2` in
   goken), the -4 being the mandatory delay slot.
   Also completed case 11 alongside this (it was only handling JAL's
   variant, `JAL {Absolute _}`; optab.c shows plain unconditional
   `JMP label` -- as opposed to case 18's `JMP (r)` indirect form --
   shares the exact same oprange 11, just without linking) since the
   case6_mips.s fixture needs unconditional jumps for its control
   flow anyway.
   Added tests/linker/mips_diff/case6_mips.s (BEQ, BNE, BGTZ, BLTZ,
   each on its taken path, accumulating 1+2+4+8=15 into the exit
   code; the not-taken path sets an out-of-range wrong value so a
   real branch/delay-slot bug would show up in the qemu-mips exit
   code, not just in the byte diff). PASS functionally (exit code 15
   both sides) but NOT byte-identical (goken: 414 bytes, xix: 430
   bytes, diff spread across several delay slots) -- same root cause
   as call_mips.s's known JAL diff: goken's sched.c hoists real
   instructions into branch delay slots instead of this port's plain
   NOPs, and with 8 branches/jumps in this fixture (4 conditional + 4
   unconditional JMP) there are more delay slots for it to fill than
   call_mips.s's single JAL. Deliberately not chasing this (same
   decision as call_mips.s: the scheduler is a real, much bigger
   subsystem for a payoff limited to a few bytes on already-correct
   code) -- case6_mips.s is kept as a fixture but left out of
   test-mips.sh's CASES list, run manually with scripts/diff-mips.sh.
   Verified: full MIPS + ARM baselines and make test (134/134) all
   still pass.
** DONE case 4 (fixed a real bug) + case 10  add $con,[r1],r2
   Codegenv.ml's case 4 previously had a standing TODO
   ("C_ADD0CON vs C_ANDCON generate different opcodes") and just
   always emitted a single op_irr, truncating the immediate via
   `land 0xffff` regardless of its actual size -- a real,
   previously-latent bug: e.g. `ADD $0x8000,R1,R2` would have
   silently encoded as ADDI $-0x8000 (wrong sign) instead of the
   REGTMP-based +0x8000 goken actually uses. Root-caused via
   goken's span.c aclass()/cmp(): case 4's needed class
   (C_ADD0CON) is reached whenever the constant fits ADDI/ADDIU's
   native sign-extended 16-bit immediate directly -- actual class
   ZCON, SCON, *or* ADDCON, i.e. the *full* [-0x8000, 0x7fff]
   range (confirmed empirically against goken with `vl -a`,
   single instruction at both ends, including exactly -0x8000).
   Outside that range (ANDCON, [0x8000, 0xffff]) needs case 10's
   REGTMP+OR expansion instead. Case 4 is now guarded by this
   exact range and case 10 added right after it for the ANDCON
   half; further out (UCON/LCON, cases 25/23, not ported) now
   correctly falls through to the generic "not handled" error
   instead of silently emitting wrong bytes.
   Note: case 10's goken C code also has an AADDU/sign-extend
   variant for *negative* values (`if(v<0) r=AADDU`), but that's
   only ever reached for AAND (AND-immediate isn't ported at all
   yet -- case 4/10 here are ADD-only, matching the pre-existing
   pattern's own restriction to `ADD (W,_sign)`), so only the
   OR/positive half of case 10 is implemented.
   Added tests/linker/mips_diff/immcon_mips.s: ADD at all four
   boundary corners (32767/-32768 for case 4, 32768/65535 for case
   10), each pair combined via ADD/SUB (case 2) into a small,
   self-checked delta via BEQ. Hit a genuine, unrelated quirk along
   the way: comparing against a plain `MOVW $-32768,R` (rather than
   combining boundary values arithmetically) makes *goken's own*
   assembler emit a literal-pool SB-relative load instead of a
   direct immediate for that one exact literal, and the resulting
   executable then segfaults under qemu-mips on goken's side --
   nothing to do with case 4/10's ADD path itself (confirmed
   separately via `vl -a` that ADD $-32768 correctly stays a single
   ADDI on goken); just avoided in the fixture, which stays about
   case 4/10 arithmetic, not that separate MOVW quirk.
   PASS functionally (exit code 7 both sides) but NOT
   byte-identical, for the same already-documented reason as
   case6_mips.s (this fixture uses BEQ/JMP to self-check, so it
   inherits the same delay-slot-scheduler diff) -- kept as a
   fixture but left out of test-mips.sh's CASES list, run manually
   with scripts/diff-mips.sh. Verified: full MIPS + ARM baselines
   and make test (134/134) all still pass.
** DONE case 12 movbs r,r / case 13 movbu r,r -- sign/zero-extending
   byte or half register move, no memory involved (Move1's first
   xix codegen match arms -- Move1 also covers register<->memory
   forms, cases 27/28/etc, not ported yet). Sign-extend (MOVB/MOVH)
   has no dedicated instruction in goken: SLL then SRA by 24 (byte)
   or 16 (half), pushing the sign bit up to bit 31 then arithmetic-
   shifting it back down. Zero-extend (MOVBU/MOVHU) is a plain
   AND-immediate mask (0xff/0xffff). New op_srr helper for
   OP_SRR(op,s,r2,r3) (shift-immediate encoding, shared with case
   16's still-unported sll $c,[r1],r2). Encoding confirmed against
   goken directly via `vl -a` before writing the fixtures.
   Added tests/linker/mips_diff/movbh_mips.s (all four forms on the
   same -1 register value, combined via SUB into a branch-free,
   fully byte-identical check) and movbh_check_mips.s (a BEQ-based
   functional check of the sign-extend side specifically -- inherits
   the same known delay-slot-scheduler diff as case6_mips.s/
   immcon_mips.s, so kept out of test-mips.sh's CASES like those).
   PASS byte-identical (movbh_mips.s) + PASS functionally (both,
   qemu-mips exit codes match). Verified: full MIPS + ARM baselines
   and make test (134/134) all still pass.
** DONE case 16 sll $c,[r1],r2 -- shift-by-immediate, the natural
   sibling of case 9 (shift-by-register) and reusing op_srr (added
   for case 12 just above). Same scoping choice as case 9: only the
   W-sized (32-bit) SLL/SRL/SRA opcodes, not goken's V-sized/vshift()
   ALAST-aliased >=32 path. Added tests/linker/mips_diff/
   case16_mips.s (SLL then SRL then SRA chained into the exit code:
   1<<4=16, 16>>2=4, 4>>1=2). PASS byte-identical + PASS same
   qemu-mips exit code. Verified: full MIPS + ARM baselines and make
   test (134/134) all still pass.
** DONE case 20 mov lohi,r / case 21 mov r,lohi / case 22 mul r1,r2
   -- MFHI/MFLO, MTHI/MTLO, and MUL. Required *new AST*, unlike
   every other MIPS case so far: goken's D_HI/D_LO pseudo-registers
   had no representation at all in Ast_asmv.ml. Added `type
   lohireg = HI | LO` and a new `LoHi of lohireg` constructor on
   `vgen` (the type's own pre-existing "| ... far more stuff"
   comment anticipated exactly this kind of extension), reusing
   Move2 rather than adding a whole new instr case -- goken's own
   optab.c only lists HI/LO under AMOVW/AMOVV, so this is a Move2
   (register-move) concern, not Move1's. Unlike ARM's Ast_asm5.ml,
   there's no hand-maintained Meta_ast_asmv.ml reflection file for
   MIPS to keep in sync -- one less file to touch than the VFP
   MOVWF/MOVFW addition earlier this session. Bumped
   Object_file.version (9->10). Grammar: new TLOHI token, plus a
   `vlgen` alternative (`vlgen: ... | TLOHI { Left (LoHi $1) }`) --
   discovered vlgen wraps `lgen`, which only derives from `gen |
   ximm`, so LoHi needed its own alternative there too, not just on
   `vgen`. Lexer: "HI"/"LO" keywords.
   Case 22 (MUL) has no destination register at all in real MIPS --
   the result always lands in HI/LO (goken's optab.c row for AMUL
   declares "to" as C_NONE, REGZERO fills that encoding slot
   unconditionally); xix's own grammar puts the 2-operand form's
   second register into ArithMul's `dest` slot rather than `r_opt`,
   but `r_opt|||rt` recovers the same value either way (confirmed
   identical to goken's own encoding via `vl -a`). Only MUL(W,_) is
   implemented -- goken's optab.c has no row at all for ADIV/AREM
   (nor their V variants), so those mnemonics are simply unsupported
   by vl itself, not just unported here; AMULU aliases the same
   oprange as AMUL (span.c's buildop()-equivalent), so both MUL
   signs work.
   Discovered along the way (empirically, via `vl -a`, not fully
   characterized): goken's sched.c pads two NOPs around certain
   MUL-result HI/LO read/write transitions -- e.g. MUL then MFLO
   then MFHI then MTLO needs no padding, but MUL then MFLO then (an
   unrelated buffer instruction) then MFHI *does*, non-monotonically
   in the buffering. This is the same already-documented, out-of-
   scope scheduler subsystem as every branch/call delay slot this
   session (sched.c), just a different corner of it -- not chased
   further, same reasoning as call_mips.s's JAL gap.
   Added three fixtures: case22_mips.s (MUL alone, no HI/LO
   readback, byte-identical), mullohi_mips.s (MTLO/MTHI/MFLO/MFHI
   round-trip with *no* MUL at all, byte-identical), and
   mullohi_check_mips.s (MUL immediately followed by MFLO,
   self-checked with BEQ -- functionally correct but inherits *both*
   the MUL/HI-LO scheduler gap *and* BEQ/JMP's own delay-slot gap,
   so kept out of test-mips.sh's CASES like the other BEQ-based
   fixtures). PASS byte-identical (case22_mips.s, mullohi_mips.s) +
   PASS functionally (all three, qemu-mips exit codes match).
   Verified: full MIPS + ARM baselines and make test (134/134) all
   still pass.
** DONE case 24 mov $ucon,,r ==> lu r (case 23/25 -- see the TODO
   just below instead, they turned out to need a different plan)
   UCON (low 16 bits all zero, magnitude outside case 3's range):
   a single LUI, no OR needed. Case 3's own guard was converted from
   an internal `constant_kind` Option-match to a direct `when` guard
   so case 24 (and the LCON fallthrough below) can sit as separate,
   later match arms on the same pattern shape.
   Tried porting case 19 for a *plain integer literal* too (not just
   the already-working Address-of-Global path further below) using
   the same LU+OR shape -- turned out to be WRONG: confirmed via
   `vl -a` directly that a genuine LCON literal (nonzero low 16
   bits, magnitude beyond ANDCON) makes goken's `va` rewrite the
   MOVW into a 4-instruction sequence loading the constant's value
   from a synthesized SB-relative data symbol (a literal pool,
   analogous to ARM's -- see Layout5.ml's pool-dedup TODO) instead
   of synthesizing it arithmetically. This is a genuine
   *assembler*-side mechanism (needs a MIPS literal pool in `ova`),
   not a Codegenv.ml/linker one -- case 19's LU+OR is only correct
   for the Address-of-Global path, which goes through a different,
   non-literal-pool-rewriting aclass() branch in goken. Reverted
   that arm; a plain out-of-range literal now falls through to the
   pre-existing generic "not handled" error instead of emitting
   wrong bytes.
   Added tests/linker/mips_diff/movw_ucon_mips.s (a positive and a
   negative UCON value). PASS byte-identical + PASS same qemu-mips
   exit code. Verified: full MIPS + ARM baselines and make test
   (134/134) all still pass.
** DONE case 23 add $lcon,r1,r2 ==> lu+or+add / case 25 add/and
   $ucon,[r1],r2 ==> lu $con,t; add t,[r1],r2 -- confirmed via `vl
   -a` beforehand (see the now-resolved TODO note this replaces)
   that ADD's immediate forms do NOT hit MOVW case 19's
   literal-pool surprise, so both ported exactly as case 4/10's
   REGTMP-based pattern already suggested: case 25 (UCON, low 16
   bits zero) is LU into REGTMP then the real op (no OR needed,
   mirroring case 24's MOVW/UCON shape); case 23 (LCON, the
   fallback for anything not ADD0CON/SCON/ANDCON/UCON) adds an OR
   step to merge in the low bits before the real op. goken's C also
   diags if p->to.reg or p->reg is REGTMP itself ("cant synthesize
   large constant") -- not replicated, a real assembler-error case
   no fixture exercises, not a byte-matching concern.
   Added tests/linker/mips_diff/case23_25_mips.s (one ADD at each
   boundary, combined via SUB -- case 2). PASS byte-identical + PASS
   same qemu-mips exit code. Verified: full MIPS + ARM baselines and
   make test (134/134) all still pass.

Not yet in Codegenv.ml at all:
** TODO case 0  pseudo ops
** TODO case 26 mov $lsext/auto/oreg,,r2 ==> lu+or+add
** TODO case 27 mov [sl]ext/auto/oreg,fr ==> lwc1 o(r)
** TODO case 28 mov fr,[sl]ext/auto/oreg ==> swc1 o(r)
** TODO case 30 movw r,fr
** TODO case 31 movw fr,r
** TODO case 32 fadd fr1,[fr2],fr3
** TODO case 33 fabs fr1,fr3
** TODO case 34 mov $con,fr ==> or/add $i,r,r2
** TODO case 37 movw r,mr
** TODO case 38 movw mr,r
** TODO case 39 rfe ==> jmp+rfe
** TODO case 40 word
** TODO case 41 movw r,fcr
** TODO case 42 movw fcr,r
** TODO case 45 case r
** TODO case 46 bcase $con,lbra
** TODO case 47 sc r, soreg
** TODO case 48 ll soreg, r

* Notes for later
** Expect a BIG/immrot-equivalent story for address-of-global (R30 bias)
   hello_linux_mips.s's `MOVW $setR30(SB), R30` is MIPS' analogue of
   ARM's `MOVW $setR12(SB), R12`. When a fixture with multiple GLOBLs
   at nonzero offsets gets added (mirroring kitchen_sink.s), expect
   to need the MIPS equivalent of the ARM BIG-bias + immrot fix in
   Codegenv.ml's address-of-global path -- check for a similar
   "less: x - BIG" style stub there first.
