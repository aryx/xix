# -*- org -*-
# Progress tracker for docs/claude_notes/notes_riscv_port_plan.txt.
# See that file for rationale/design; this file is just checklist state.

* Phase -1 -- the linker didn't exist yet
** DONE build Codegeni.ml/.mli, Layouti.ml/.mli, Rewritei.ml, Typesi.ml
   From scratch, mirroring Codegen5.ml/Codegenv.ml/Layout5.ml/
   Layoutv.ml/Rewrite5.ml/Rewritev.ml's structure and the case-N
   comment convention (goken's linkers/il/asm.c). Layouti.ml is
   simpler than the ARM/MIPS versions: RISC-V never needs a literal
   pool (large constants always inline via LUI+ADDI), so there's no
   pool/splicing bookkeeping at all.
** DONE wire CLI.ml + linker/dune + assembler/objects/Object_file.ml
   Added `linki` (mirrors link5/linkv) and Arch.Riscv to `link`'s
   dispatch and to config_of_header_type_and_flags's INITTEXT
   default (0x10000, confirmed against goken's actual entry point
   0x100a0 = 0x10000 + header_size 0xa0). Added "oil" to CLI.ml's
   argv0 arch-detection and to linker/dune's install stanza (mirrors
   how o5l/ovl already share one Main.exe). Object_file.is_obj_filename's
   regex was `.*\.o[5v]$` (ARM/MIPS only) -- added 'i' for RISC-V's .oi.
** DONE fix Elf.ml's int_of_machine (MRiscv was `failwith "TODO"`)
   Filled in with 243 (EM_RISCV, the standard ELF machine ID).
** DONE fix a real, blocking assembler bug (Parse_asmi.ml)
   The whole keyword-recognition table was a copy-pasted-then-
   commented-out ARM template, never adapted -- MOVW/ECALL (or
   anything else) couldn't even be tokenized, so nothing could
   assemble at all ("Syntax error, last name: MOVW"). Wired up
   "MOVW" -> TMOVE2 W__ and "ECALL" -> TSYSCALL (matching Ast_asmi's
   actual Move1/Move2 grammar, not ARM's single TMOV token the
   template had). MOVB/MOVH/MOVBU/MOVHU and arithmetic/branch/JAL
   mnemonics left unwired -- real backlog, not guessed at (see notes
   doc for why Move1's exact intended use isn't obvious yet).

* Phase 0 -- harness
** DONE diff harness script (assemble+link both sides, cmp -l, qemu-riscv32)
   scripts/diff-riscv.sh + test-riscv.sh, mirrors the ARM/MIPS ones.
   goken's il needs `-c` (disable RVC instruction compression --
   see below) `-s` (strip native symbol table, same as ARM/MIPS).
   No -H flag needed on either side (il defaults to ELF already,
   like vl; oil's CLI.ml always defaults to "elf" regardless of arch).
** DONE discover goken's il compresses instructions (RVC) by default
   linkers/il/compress.c, called unconditionally from asm.c's encode
   loop unless `-c` is passed (`if(!debug['c'] && o->ctype)`) --
   found by disassembling goken's default output and hitting 16-bit
   opcodes a plain-RV32 disassembler couldn't decode (e.g. bytes
   `05 45` = `C.LI a2,13`, replacing a 4-byte `addi a2,zero,13`).
   Decided NOT to implement compression in xix (substantial separate
   feature: deciding what's losslessly shrinkable, then re-doing
   layout since it changes instruction sizes/addresses). The harness
   always passes goken's `-c` so the comparison is apples to apples.

* Phase 0.5 -- baseline corpus
** DONE tests/linker/riscv_diff/hello_linux.s: cmp -l clean + qemu-riscv32
   Copied from goken's tests/s/mini/ (simpler than xix's own
   tests/assembler/hello_linux.s, which adds extra stuff not
   needed for this baseline). PASS: byte-identical (4112 bytes both
   sides) on the very first successful link, once the assembler bug,
   the -c flag, and the BIG/fits_addi_imm address-of-global formula
   were all in place -- no separate "found via a second fixture" bug
   hunt needed this time, unlike ARM/MIPS's kitchen-sink stories.
   Both sides print "Hello, world" and exit 0 under qemu-riscv32.
   This fixture already exercises BOTH the address-of-global fast
   path (`MOVW $msg(SB),R11`) and slow path (`MOVW $setSB(SB),RSB`,
   forced there by the `final_offset <> 0` exclusion, not by a range/
   encoding failure like ARM's setR12 case -- see notes doc).

* Phase 1 -- asm.c case backlog (goken/linkers/il/asm.c)
Cases with a matching (* case N: *) comment already in Codegeni.ml:
** DONE case 2  addi $I,[R,]D (used for the prologue/epilogue SP
   adjustment in Rewritei.ml, and for MOVW-immediate-fits-12-bit).
   Later generalized (same case number) to the rest of the OP-IMM
   family -- andi/ori/xori/slti/sltiu $I,[R,]D -- reusing
   oprrr_arith_opcode's funct3 (SUB has no immediate form; SLL/SRL/
   SRA's immediate forms are case 1, a different bit layout). Tested
   via arith_imm_case2.s (+ riscv64_diff copy): PASS byte-
   identical + PASS same exit code on both RV32 and RV64. This also
   indirectly validates case 10's MOVBU-via-ANDI encoding below,
   which reuses the exact same op_itype/op_opimm/funct3=7 path.
** DONE case 6  sb R,I(S) (store; needed by the link-register-save
   prologue) / case 7  lb I(S),D (load; needed by the link-register-
   restore epilogue). Later generalized (same case numbers) to
   Move1's B_/H_ byte/half loads/stores too (e.g. user-written
   "MOVB R,I(S)"/"MOVB I(S),D"), reusing new op_stype/gen_store/
   gen_load helpers -- goken's case 6/7 are fully generic over
   SB/SH/SW/SD and LB/LBU/LH/LHU/LW/LD, the funct3 difference coming
   purely from which mnemonic's optab entry was looked up, not from
   the C code itself. Required extending the `gen` grammar
   (Parser_asmi.mly) with a `con TOPAR reg TCPAR -> Indirect` rule
   (mirroring Parser_asmv.mly/MIPS) since Indirect was previously
   only ever constructed programmatically by Rewritei.ml's prologue/
   epilogue, unreachable from a .s file. Tested via
   mem_move_case6_7.s (+ riscv64_diff copy): PASS byte-identical + PASS
   same exit code on both RV32 and RV64.
   Found and fixed a second, more serious pre-existing bug while
   writing this fixture (the first to use a nonzero, non-multiple-
   of-8 leaf frame size): Rewritei.ml's frame-size computation was
   missing goken's obj.c padding step entirely (round declared size,
   then bump by a literal 4 if the size's bit 2 has the "wrong"
   parity -- the parity check itself is inverted between riscv32 and
   riscv64), and separately was treating goken's `ptrsize` (the
   RLINK-slot size added on top of the padded declared size to get
   the actual SP-adjust amount) as a flat 4 on both arches, when
   goken's own obj.c sets it to *8* on riscv64 (thechar=='j') --
   RLINK is a full 8-byte pointer there. Fixed by threading `is_64`
   into `Rewritei.rewrite` (CLI.ml) and adding `padded_declared_size`
   and an arch-aware `ptrsize` function. Also had to special-case the
   size-declared-as-0-but-non-leaf frame (needs room to spill RLINK
   even with no real locals): goken's noop.c forces this to exactly
   8 on *both* arches via a branch that only fires `ptrsize == 4`
   (i.e. "2*ptrsize" is a riscv32-only workaround; riscv64's ptrsize
   is already 8, no bump needed) -- naively using `2 * ptrsize is_64`
   for both arches would silently double-count riscv64's case to 16,
   which is exactly the regression jal_case4.s's own frame shape
   (JAL-calling, `$0` declared) caught when this fix was first tried.
** DONE case 9  lui I1,D; addi I0,D (MOVW-immediate too big for 12 bits)
** DONE case 11 addi $I,R,D (address-of-global fast path)
** DONE case 20 lui/auipc I1,D; addi I0;D (address-of-global slow/
   absolute path; also used for address-of-procedure/STText2)
** DONE case 24 SYS (ECALL)
** DONE case 0  add S,[R,]D (ADD/SUB/AND/OR/XOR/SLT/SLTU/SLL/SRL/SRA,
   register-register) / case 1  slli $I,[R,]D (SLL/SRL/SRA,
   shift-by-immediate) -- the RV32-native forms only (`w option` =
   None); the explicit-32-bit-on-RV64 *W variants (ADDW/SLLW/etc,
   a separate opcode family, OOP_32 not OOP) are a follow-up.
   goken's default-middle-register rule (asm.c: `if(r==NREG) ...
   default: r=p->to.reg`) matches the `middle ||| rt` convention
   already used by case 2. New op_rtype helper (R-type encoding) and
   oprrr_arith_opcode (funct3/funct7 per mnemonic, also reused by
   case 1's shift-immediate encoding, which packs the same funct7
   bit into the immediate's own [11:5] bits). Confirmed via `il -a`
   directly that OP_R's rs1 is the *middle* operand and rs2 the
   *from* one (not the reverse) -- e.g. "ADD R1,R2,R3" encodes
   rs1=R2(middle), rs2=R1(from), rd=R3.
   Also wired up the ADD/SUB/SLL/SRL/SRA/SLT/SLTU/XOR/OR/AND
   (TARITH) and MUL/DIV/DIVU/REM/REMU (TMULOP) keywords in
   Parse_asmi.ml -- none of them were wired at all before this
   (only MOVW/ECALL were). MULH/MULHSU/MULHU left unwired: Ast_asmi's
   mul_opcode has no constructor for them yet (`MUL` alone, with a
   standing "TODO: lots of MUL").
   Added tests/linker/riscv_diff/arith_shift_case0_1.s (all ten register-
   register ops plus SLL/SRL/SRA-by-immediate, chained into the exit
   code) and its riscv64_diff copy (arith_shift_case0_1.s, since
   Codegeni.ml is fully shared between oil/ojl and nothing here is
   RV64-specific). PASS byte-identical + PASS same exit code on
   both RV32 and RV64. Verified: full ARM/MIPS baselines and make
   test (134/134) all still pass.

** DONE case 3  beq S,[R,]L (BEQ/BNE/BLT/BGE/BLTU/BGEU, both the
   2-register and 1-register-vs-zero forms). New op_btype (B-type
   encoding, ported bit-scatter-for-bit-scatter from goken's OP_B
   macro, same reasoning as case 0/1's op_rtype) and
   opirr_bxx_funct3 (funct3 per condition; LE/GT left unwired --
   real RISC-V has no BLE/BGT hardware instruction, only an
   operand-swapping pseudo-op this port doesn't add).
   Extended Ast_asmi's `Bxx` from
   `b_condition * gen * A.branch_operand` to
   `b_condition * gen * reg option * A.branch_operand` (bumped
   Object_file.version 14->15) since, unlike MIPS's split BEQ/BNE-
   vs-Bxx AST design, RISC-V's 6 hardware branches are ALL genuine
   2-register comparisons per goken's uniform case-3 handling --
   Parser_asmi.mly gained a second `TB gen TC reg TC rel` rule for
   the explicit-middle-register form.
   Discovered by reading goken's own yacc grammar (ia/a.y), not just
   asm.c: the 1-register form (`BEQ S,L`, no explicit middle reg) is
   rewritten AT PARSE TIME to `from=regzero, reg(middle)=S` --
   i.e. the explicit register lands in the *middle* slot, not
   `from`. This is NOT the usual "middle defaults to zero" pattern
   every other optional-middle-register case uses (MIPS's case 2/9/
   0/1, RISC-V's own case 0/1) -- confirmed by decoding the actual
   instruction bytes (goken's disassembler text is ambiguous/
   misleading about operand order), not just reading the C source.
   Also newly discovered (out of scope, documented at length in
   branch_case3.s's own comment): goken's assembler recognizes the
   "branch to L1; immediately followed by unconditional JMP to L2"
   idiom (a classic if/then/else compiler pattern) and rewrites it --
   inverts the branch to jump straight to L2, then *relocates* L1's
   code to the end of the function, threaded back in with its own
   JMP. A real code-layout/CFG optimization in goken's own assembler,
   not something this port replicates; worked around by using an
   "invert-and-skip-one-instruction" idiom instead (branch directly
   over a single following instruction, no intervening JMP), which
   doesn't trigger it.
   Added tests/linker/riscv_diff/branch_case3.s (all six conditions,
   both operand forms, chained into the exit code) and its
   riscv64_diff copy. PASS byte-identical + PASS same exit code (127)
   on both RV32 and RV64.
** DONE case 4  jal [D,]L (JAL with an explicit, non-RLINK link
   register -- xix's own grammar routes "JAL reg,label" through the
   JALR AST constructor, `TJAL reg TC branch {JALR($2,$4)}`, which is
   why Codegeni.ml's match arm is `JALR (rd, {contents=Absolute _})`
   rather than a separate constructor; goken's own "JAL" mnemonic
   always requires an explicit register too, via the same grammar
   token as JALR -- a bare "JAL label" with no register doesn't even
   parse in goken's own assembler). New op_jtype (J-type encoding,
   same bit-scatter-porting approach as op_btype) and branch_delta
   helper (shared by case 3 and case 4).
   Found and fixed a real, pre-existing bug while testing this: case
   6's store (`Move2 (W__, ...)`, used by Rewritei.ml's non-leaf
   prologue to save RLINK -- never byte-verified against goken before
   since no earlier fixture triggered the non-leaf prologue path) had
   funct3 hardcoded to 0 (SB, byte store) instead of matching the
   move's actual width -- silently storing only 1 byte of the return
   address instead of a full word. Fixed to SW (funct3=2) on riscv32;
   testing the same fixture on riscv64 then caught a second instance
   of the same class of bug (RLINK is a full pointer there, so the
   save/restore need SD/LD, funct3=3, not SW/LW) -- both case 6
   (store) and case 7 (load) now branch on `is_64`.
   Deliberately NOT covered by a byte-identical fixture: a plain
   "JMP label" to a non-adjacent target, and JMP's interaction with
   conditional branches/loops -- every natural shape collides with
   one of goken's own code-layout optimizations (the dead-code-after-
   JMP elision already known from MIPS's RFE case; the branch+jmp
   inversion from case 3; and, combined with a later JAL/indirect-
   return in the same function, a "loop rotation" that was observed
   via an ad-hoc test to miscompute a target address into a non-4-
   byte-aligned garbage branch). See jal_case4.s's own comment.
   Added tests/linker/riscv_diff/jal_case4.s and its riscv64_diff
   copy. PASS byte-identical + PASS same exit code on both RV32 and
   RV64. Verified: full ARM/MIPS/RISC-V baselines and make test all
   still pass.

Not yet in Codegeni.ml at all:
** DONE case 5  jalr D,I(S) / jmp I(S). New `JALRI of reg * reg *
   A.offset` (bumped Object_file.version 26->27), generalizing the
   RET-only `JMP (IndirectJump _)` arm (offset always 0, rd always
   REGZERO) to an arbitrary offset and rd. Wired "JALR" as an alias
   keyword for the existing TJAL token (mirrors goken's own lex.c,
   which maps both "JAL" and "JALR" to the identical AJAL token,
   dispatching on operand shape alone) plus two new grammar
   productions: "JMP I(S)" (rd defaults to REGZERO, a true jump) and
   "JAL/JALR D,I(S)" (explicit D).
   Found and fixed a real bug while testing this: Rewritei.ml's leaf
   detection only cleared LEAF for `JAL`/`JALR` (the label-targeted
   forms), never for the new `JALRI` -- so a function making a real
   call via "JALR D,I(S)" was still misclassified as a leaf, missing
   its RLINK-save prologue entirely (confirmed via byte-diff: goken's
   own output had a 2-instruction prologue this port's output lacked
   until fixed). Fixed by clearing LEAF for `JALRI (rd,_,_)` whenever
   `rd <> rZERO` (rZERO is only ever the JMP-spelled default in this
   port's own grammar, never user-overridable, so it's an exact proxy
   for goken's own AJAL-vs-AJMP mnemonic-identity check).
   Also found, but deliberately NOT fixed here (separate from case 5
   itself): testing this case's fixture via "$proc(SB)" (address-of-
   procedure) surfaced a real RV64-specific gap -- goken uses AUIPC
   (PC-relative) for a procedure/STText2 address on RV64, this port
   still emits an absolute LUI+ADDI pair there (functionally correct,
   confirmed by matching exit codes, but not byte-identical). Worked
   around in the fixture by reaching both test callees via the
   already-verified case 4 JAL mechanism instead of an address-of-
   procedure load. Revisit Codegeni.ml's case 9/20 (address-of-
   global/procedure) AUIPC-vs-LUI dispatch for RV64 specifically if a
   fixture ever needs "$proc(SB)" directly.
   Added tests/linker/riscv_diff/jalr_case5.s (+ riscv64_diff copy):
   PASS byte-identical + PASS same exit code (22) on both RV32 and
   RV64.
** DONE case 8  lui I,D. New `LUI of int * reg` AST constructor
   (bumped Object_file.version 15->16) and `TLUI`/"LUI" grammar/
   lexer wiring for xix's own `LUI $I,D` syntax, plus a fast path in
   the existing `Move2 (W__, Right (Int i), Gen (GReg rt))` case
   (MOVW-immediate) that emits case 8's bare 4-byte LUI instead of
   case 9's 8-byte LUI+ADDI whenever `i land 0xfff = 0` -- matching
   goken's own constant classifier (il/span.c's `aclass`, C_UCON
   class), which picks the same shorter encoding since there's
   nothing for the ADDI to add.
   Discovered while trying to test xix's own `LUI $I,D` mnemonic
   directly against goken: goken's grammar parses a standalone "LUI"
   mnemonic (LLUI -> ALUI) but its linker optab.c has no entry for
   ALUI at all ("illegal combination LUI..." -- dead syntax in
   goken's own assembler, same category as case 4's bare "JAL label"
   having no working goken syntax either). So case 8 is only tested
   through the MOVW-immediate path that actually reaches it in
   goken, via tests/linker/riscv_diff/lui_case8.s (+ riscv64_diff
   copy); xix's own directly-usable `LUI $I,D` mnemonic is kept as a
   building block with no differential test of its own, same
   reasoning as JAL's bare-label form. PASS byte-identical + PASS
   same exit code on both RV32 and RV64.
** DONE case 10 sign extend -- register-to-register MOVB/MOVBU/MOVH/
   MOVHU (e.g. "MOVB R1,R2"), disambiguated from case 6/7's memory
   access above by the `from` gen being GReg here rather than
   Indirect. MOVBU is special-cased by goken as a single ANDI $0xFF
   (funct3=7, reusing case 2's generalized op_itype/op_opimm path
   above); MOVB/MOVH/MOVHU all go through an SLLI-then-SRLI/SRAI
   shift pair instead (shift left by 24 or 16 to punt the byte/half
   up to the top of the register, then shift back down -- arithmetic
   for sign-extend, logical for zero-extend -- widened by +32 on
   riscv64 since the trick needs the *top* of a 64-bit register, not
   a 32-bit one).
   Discovered goken's own grammar has NO register-to-register syntax
   for its LMOVB-token mnemonics at all (`LMOVB rreg ',' addr` / `addr
   ',' rreg`, where `addr` = `oreg | name`, never a bare register) --
   even though the underlying machinery (asm.c's case 10, optab.c's
   `AMOVB C_ZREG C_REG` entry) fully supports a register "from"
   operand (`C_ZREG` accepts `C_REG` per span.c's `cmp()`). So this
   case, like case 4's bare "JAL label" and case 8's standalone
   "LUI", has real, reachable machinery in goken's own linker with no
   matching concrete syntax to differentially test against -- xix's
   MOVB/MOVH/MOVBU/MOVHU register-to-register syntax is kept as a
   real, directly-usable building block, but its correctness rests
   on reusing the exact same already-byte-verified encoding helpers
   as case 1 (SLLI/SRAI shift-immediate) and case 2 (ANDI), not on a
   dedicated differential fixture of its own.
** DONE case 12/13 mov r,lext / mov lext,r => lui/auipc I1,T; s[bhwd]/l[bhwd] r,I0(T)
   Found this was a bigger gap than the case list suggested: "MOVW
   R,sym(SB)" / "MOVW sym(SB),R" (storing/loading a global's *value*
   by symbol, as opposed to the already-done case 9/11/20 address-of)
   had ZERO codegen at all, not just the large-offset slow path --
   `Ast_asmi.ml`'s `Entity` gen constructor existed but nothing built
   it (`gen`'s grammar had no `name -> Entity` rule) and nothing
   consumed it in Codegeni.ml. Implemented both the small-offset fast
   path (reuses case 6/7's shape with RSB as the base register,
   confirmed via optab.c: the same C_SOREG class a plain "sb R,I(S)"
   and a small-offset "sb R,sym(SB)" both resolve to) and the
   large-offset slow path proper (new `gen_upper_and_low_via` helper:
   same LUI/AUIPC rounding as case 9/20's `gen_absolute_via`, but
   hands the low 12 bits back to the caller to fold directly into the
   store/load's own immediate field instead of a separate ADDI).
   Found and fixed a real bug while testing: "MOVW" always means
   32-bit width regardless of arch (goken's own optab.c:
   `AMOVW,...,OSTORE,2` is unconditional, not is_64-gated) -- the
   pre-existing case 6/7 helper is_64-branches its own funct3 only
   because it's *also* reused internally by Rewritei.ml's own
   pointer-width RLINK save (never reachable from real .s source);
   the new, directly-user-reachable case 12/13 code must not (and now
   doesn't) inherit that.
   Also found, but deliberately NOT fixed here (separate from case
   12/13's own codegen correctness): combining two GLOBLs of very
   different sizes (a small `foo` and an 8192-byte `bigbuf`) in one
   program shifts `foo`'s own resolved SB-relative offset by 4 bytes
   between goken and xix -- a data-segment layout/alignment
   discrepancy, not a case 12/13 encoding bug (confirmed: each global
   alone, in its own fixture, is fully byte-identical). Worked around
   by keeping the two new fixtures' globals to one each; revisit
   Layouti.ml's own data-segment layout against goken's `dodata()` if
   a fixture ever needs multiple differently-sized GLOBLs in one
   program (same category of issue as mips_port.md's own
   "GLOBL-without-DATA total file size mismatch" open issue).
   Added tests/linker/riscv_diff/sb_value_case6_7.s and
   sb_value_case12_13.s (+ riscv64_diff copies): PASS byte-identical +
   PASS same exit code on both RV32 and RV64.
** DONE case 14 op $lcon,[r,]d => lui L1,T; addi $L0,T,T; op T,r,d
   ADD/AND/OR/XOR only -- confirmed via optab.c that only these 4 have
   a C_LCON row reaching case 14 (no SUB/SLT/SLTU/SLL/SRL/SRA row
   exists): `OP_RO(r,REGTMP,rt)`'s own encoding has no funct7 term at
   all (always 0), matching exactly the 4 ops whose register-register
   funct7 is already 0. Reuses `gen_upper_and_low_via` (introduced for
   case 12/13) to materialize the constant into REGTMP via LUI+ADDI,
   then the real op register-register with REGTMP as the "from"
   operand. The RV64-only "awkward edge case" for v>=0x7ffff800
   (goken's own comment) isn't handled -- narrower-but-real scoping,
   same as MULH/MULHSU/MULHU/CSRRWI etc elsewhere in this port.
   Added tests/linker/riscv_diff/lcon_case14.s (+ riscv64_diff copy):
   PASS byte-identical + PASS same exit code on both RV32 and RV64.
** DONE case 15/16 mov r,L(s) / mov L(s),r => lui L1,T; add s,T,T; s[bhwd]/l[bhwd] r,L0(T)
   Large offset relative to an arbitrary base register (unlike case
   12/13's SB-specific slow path, no INITDAT/BIG bias -- goken's
   `OP_ADD(r,REGTMP,REGTMP)` just adds the base register at runtime).
   Generalized `gen_store`/`gen_load` (previously a thin "check offset
   fits, else error" wrapper around a caller-supplied encoder thunk)
   to take the funct3/registers directly and internally pick the
   small-offset (case 6/7) or large-offset (case 15/16) shape itself
   -- shared by all 6 existing call sites (Move2 W__/V__, Move1
   B_/H_), so case 15/16 support "fell out" for all of them at once,
   not just W__.
   Found and fixed a real, confirmed bug while testing: Rewritei.ml's
   own link-register save/restore used move_size W__ ("MOVW", always
   32-bit per goken's own optab.c) when it actually wants
   pointer-width behavior (bare "MOV", is_64-dependent, confirmed
   empirically: goken emits SW for bare "MOV" on riscv32 and SD on
   riscv64) -- conflating the two in one shared encoder arm was
   harmless until a genuine large-offset user "MOVW" fixture on
   riscv64 needed the *other* (always-32-bit) behavior from the exact
   same tag. Fixed by giving `Move2 (W__, ...)` and `Move2 (V__, ...)`
   their own separate Codegeni.ml arms (W__ always funct3=2, V__
   is_64-dependent) and switching Rewritei.ml's RLINK save/restore to
   build `V__` (not `W__`) nodes.
   Added tests/linker/riscv_diff/lreg_case15_16.s (+ riscv64_diff
   copy): PASS byte-identical + PASS same exit code on both RV32 and
   RV64 (this fixture is what originally caught the RLINK bug above,
   on RV64 specifically).
** TODO case 17 fcvt S,D
** TODO case 18 lui L1, T; jal [r,]L0(T)
** DONE (confirmed dead in goken, not implemented) case 19 addiw $0, rs, rd. Investigated: goken's optab has a
   real `AMOVW C_REG C_REG` entry mapping to this (ADDIW, zero
   immediate, used to re-sign-extend a 32-bit value already sitting
   in a register on riscv64), but obj.c *rewrites* AMOVW/AMOVWU to
   plain AMOV whenever NEITHER operand is a memory reference
   (`if(from.type!=D_OREG && to.type!=D_OREG) o=AMOV`), on *both*
   riscv32 and riscv64 -- confirmed empirically ("MOVW R5,R6" on
   both `ia`/`il` and `ja`/`jl` produces a plain ADD-with-REGZERO
   encoding, opcode 0x33, never ADDIW's 0x1B). So this optab entry
   looks unreachable via any concrete goken syntax at all, same
   category as case 4's bare "JAL label"/case 8's standalone "LUI"/
   case 10's register-to-register MOVB -- deferred rather than wired
   with no way to verify it.
** TODO case 21 lui I,D; s[lr]ai N,D
** DONE case 22 CSRRx C, rs, rd. New `csr_op = CSRRW | CSRRS | CSRRC`
   and `CSR of csr_op * int * reg * reg` (bumped Object_file.version
   16->17), with "CSR" wired as its own keyword (TCSRTOK) separate
   from the CSRRW/CSRRS/CSRRC mnemonics (TCSR), mirroring goken's own
   lexer split (LCTL vs LCSR) -- concrete syntax "CSRRW
   CSR($num),S,D". funct3 (1/2/3 for W/S/C) reuses the existing
   op_itype/op_system encoding, same opcode family as ECALL. The
   immediate variants (CSRRWI/CSRRSI/CSRRCI) aren't wired, narrower-
   but-real scoping matching MULH/MULHSU/MULHU's precedent.
   Tested via system_csr_case22.s (+ riscv64_diff copy): PASS byte-
   identical on both RV32 and RV64; the actual CSR access traps as
   "Illegal instruction" under qemu-user on both goken and xix
   equally (expected -- CSR access needs kernel/privileged mode,
   which qemu-user doesn't emulate), so this only confirms the
   encoding, the same reasoning as why ECALL's own tests never
   inspect kernel-side state either.
** DONE case 25 word $x / case 26 pseudo ops -- both already routed
   through the arch-independent Codegen.default_rules (the
   `T.Virt _ | T.TEXT _ | T.WORD _ -> Codegen.default_rules ...`
   catch-all already at the top of `rules`, same shared code path
   ARM/MIPS use), confirmed via tests/linker/riscv_diff/
   word_pseudo_case25_26.s (+ riscv64_diff copy) with no new code needed --
   same "confirmed no new code needed" precedent as MIPS's case 0/
   case 40. PASS byte-identical + PASS same exit code on both RV32
   and RV64.

* Notes for later
** DONE (stale note, already resolved) MOVB/MOVH/MOVBU/MOVHU wiring
   Resolved by case 6/7/10's own work above: MOVB/MOVH/MOVBU/MOVHU
   are wired (TMOVE1, Parse_asmi.ml), both memory (case 6/7,
   generalized) and register-to-register (case 10) forms.
** DONE Rewritev.ml (MIPS) leaf-with-locals RET bug -- confirmed real, fixed
   Checked goken's vl/noop.c directly: it DOES have the same 3-way
   leaf/frame distinction as il/noop.c. Rewritev.ml's RET expansion
   only had a 2-way split, unconditionally doing a load-restore-jump
   epilogue even for a leaf-with-locals procedure whose prologue never
   wrote anything to restore -- confirmed as a real, live segfault
   (not just theoretical), reproduced by temporarily reverting the fix
   and re-running the new regression fixture under qemu-mips. Fixed by
   threading `(autosize, needs_link_save)` through step2, same shape
   as Rewritei.ml's own `frame` type. See
   tests/linker/mips_diff/leaf_locals_ret_check.s (a `_check` fixture,
   not byte-identical -- goken's sched.c hoists the epilogue's
   SP-restore into RET's own delay slot, same documented scheduler gap
   as everywhere else in this port) and mips_port.md's "Post-completion
   fix" section.
